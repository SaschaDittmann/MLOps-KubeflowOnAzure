apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: azure-files-pvc
  namespace: kubeflow
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: azurefile
  resources:
    requests:
      storage: 5Gi
